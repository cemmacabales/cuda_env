version: '3.8'

services:
  cuda-dev:
    image: nvidia/cuda:12.4.1-devel-ubuntu22.04
    container_name: cuda-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: /bin/bash
